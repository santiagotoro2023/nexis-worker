<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose takes over all rendering; use DeviceDefault as the base
         so no AppCompat dependency is required. -->
    <style name="Theme.NexisWorker" parent="android:Theme.DeviceDefault.NoActionBar" />
</resources>
